using MonoMod.RuntimeDetour;
using MonoMod.RuntimeDetour.HookGen;

namespace HKMirror.Hooks.OnHooks;

/// <summary>
///     Contains different types of On Hooks for RotateTo class.<br />
///     Contains hooks that aren't generated by monomod, and before and after orig hooks for easier hooking.
/// </summary>
public static class OnRotateTo
{
    internal static class HookHandler
    {
        private static readonly List<string> HookedList = new();

        internal static void HookReset()
        {
            if (!HookedList.Contains("Reset"))
            {
                HookedList.Add("Reset");
                new Hook(ReflectionHelper.GetMethodInfo(typeof(RotateTo), "Reset"), Reset);
            }
        }

        internal static event Delegates.Reset_BeforeArgs _beforeReset;
        internal static event Delegates.Reset_NormalArgs _afterReset;

        private static void Reset(Action<RotateTo> orig, RotateTo self)
        {
            Delegates.Params_Reset @params = new()
            {
                self = self
            };
            _beforeReset?.Invoke(@params);
            self = @params.self;
            orig(self);
            if (_afterReset != null) _afterReset.Invoke(@params);
        }

        internal static void HookOnUpdate()
        {
            if (!HookedList.Contains("OnUpdate"))
            {
                HookedList.Add("OnUpdate");
                new Hook(ReflectionHelper.GetMethodInfo(typeof(RotateTo), "OnUpdate"), OnUpdate);
            }
        }

        internal static event Delegates.OnUpdate_BeforeArgs _beforeOnUpdate;
        internal static event Delegates.OnUpdate_NormalArgs _afterOnUpdate;

        private static void OnUpdate(Action<RotateTo> orig, RotateTo self)
        {
            Delegates.Params_OnUpdate @params = new()
            {
                self = self
            };
            _beforeOnUpdate?.Invoke(@params);
            self = @params.self;
            orig(self);
            if (_afterOnUpdate != null) _afterOnUpdate.Invoke(@params);
        }

        internal static void HookDoRotateTo()
        {
            if (!HookedList.Contains("DoRotateTo"))
            {
                HookedList.Add("DoRotateTo");
                new Hook(ReflectionHelper.GetMethodInfo(typeof(RotateTo), "DoRotateTo"), DoRotateTo);
            }
        }

        internal static event Delegates.DoRotateTo_BeforeArgs _beforeDoRotateTo;
        internal static event Delegates.DoRotateTo_NormalArgs _afterDoRotateTo;

        private static void DoRotateTo(Action<RotateTo> orig, RotateTo self)
        {
            Delegates.Params_DoRotateTo @params = new()
            {
                self = self
            };
            _beforeDoRotateTo?.Invoke(@params);
            self = @params.self;
            orig(self);
            if (_afterDoRotateTo != null) _afterDoRotateTo.Invoke(@params);
        }
    }

    /// <summary>
    ///     Contains necessary information to create Hooks. Does not contain any hooks
    /// </summary>
    public static class Delegates
    {
        public delegate void DoRotateTo_BeforeArgs(Params_DoRotateTo args);

        public delegate void DoRotateTo_NormalArgs(Params_DoRotateTo args);

        public delegate void OnUpdate_BeforeArgs(Params_OnUpdate args);

        public delegate void OnUpdate_NormalArgs(Params_OnUpdate args);

        public delegate void Reset_BeforeArgs(Params_Reset args);

        public delegate void Reset_NormalArgs(Params_Reset args);

        public sealed class Params_Reset
        {
            public RotateTo self;
        }

        public sealed class Params_OnUpdate
        {
            public RotateTo self;
        }

        public sealed class Params_DoRotateTo
        {
            public RotateTo self;
        }
    }

    /// <summary>
    ///     Contains Hooks to that run code before orig(self) is called
    /// </summary>
    public static class BeforeOrig
    {
        public static event Delegates.Reset_BeforeArgs Reset
        {
            add
            {
                HookHandler._beforeReset += value;
                HookHandler.HookReset();
            }
            remove => HookHandler._beforeReset -= value;
        }

        public static event Delegates.OnUpdate_BeforeArgs OnUpdate
        {
            add
            {
                HookHandler._beforeOnUpdate += value;
                HookHandler.HookOnUpdate();
            }
            remove => HookHandler._beforeOnUpdate -= value;
        }

        public static event Delegates.DoRotateTo_BeforeArgs DoRotateTo
        {
            add
            {
                HookHandler._beforeDoRotateTo += value;
                HookHandler.HookDoRotateTo();
            }
            remove => HookHandler._beforeDoRotateTo -= value;
        }
    }

    /// <summary>
    ///     Contains Hooks to that run code after orig(self) is called
    /// </summary>
    public static class AfterOrig
    {
        public static event Delegates.Reset_NormalArgs Reset
        {
            add
            {
                HookHandler._afterReset += value;
                HookHandler.HookReset();
            }
            remove => HookHandler._afterReset -= value;
        }

        public static event Delegates.OnUpdate_NormalArgs OnUpdate
        {
            add
            {
                HookHandler._afterOnUpdate += value;
                HookHandler.HookOnUpdate();
            }
            remove => HookHandler._afterOnUpdate -= value;
        }

        public static event Delegates.DoRotateTo_NormalArgs DoRotateTo
        {
            add
            {
                HookHandler._afterDoRotateTo += value;
                HookHandler.HookDoRotateTo();
            }
            remove => HookHandler._afterDoRotateTo -= value;
        }
    }

    /// <summary>
    ///     Contains all On Hooks, even those that aren't in the On namespace such as API generated functions and property
    ///     getters/setters
    /// </summary>
    public static class WithOrig
    {
        public static event Delegates.Reset_NormalArgs Reset
        {
            add => HookEndpointManager.Add<Delegates.Reset_NormalArgs>(
                ReflectionHelper.GetMethodInfo(typeof(RotateTo), "Reset"), value);
            remove => HookEndpointManager.Remove<Delegates.Reset_NormalArgs>(
                ReflectionHelper.GetMethodInfo(typeof(RotateTo), "Reset"), value);
        }

        public static event Delegates.OnUpdate_NormalArgs OnUpdate
        {
            add => HookEndpointManager.Add<Delegates.OnUpdate_NormalArgs>(
                ReflectionHelper.GetMethodInfo(typeof(RotateTo), "OnUpdate"), value);
            remove => HookEndpointManager.Remove<Delegates.OnUpdate_NormalArgs>(
                ReflectionHelper.GetMethodInfo(typeof(RotateTo), "OnUpdate"), value);
        }

        public static event Delegates.DoRotateTo_NormalArgs DoRotateTo
        {
            add => HookEndpointManager.Add<Delegates.DoRotateTo_NormalArgs>(
                ReflectionHelper.GetMethodInfo(typeof(RotateTo), "DoRotateTo"), value);
            remove => HookEndpointManager.Remove<Delegates.DoRotateTo_NormalArgs>(
                ReflectionHelper.GetMethodInfo(typeof(RotateTo), "DoRotateTo"), value);
        }
    }
}